interface:
  display_name: "Edgeever"
  short_description: "Repo-specific patterns and workflows for edgeever"
  default_prompt: "Use the edgeever repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true